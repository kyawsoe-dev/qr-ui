# QR Code Frontend

A simple web application that displays a user loyalty QR code by fetching it from a backend API.

## Overview

This frontend retrieves and displays a QR code for user loyalty programs. It connects to a backend service running on `localhost:3000` to fetch the QR code image.

## Features

- Displays user loyalty QR code
- Simple and clean UI
- Fetches QR code from backend API

